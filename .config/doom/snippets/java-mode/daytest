# -*- mode: snippet -*-
# name: daytest 
# key: daytest
# --

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.RegisterExtension;

import nl.arjenwiersma.aoc.common.DayInputExtension;

public class Day$1Test  {
    
    @RegisterExtension
    private final DayInputExtension extension = new DayInputExtension($1);

    Day$1 day = new Day$1();

    @Test
    public void testPart1() {
        assertThat(day.part1(extension.getSampleLines())).isEqualTo(1);
        // assertThat(day.part1(extension.getLines())).isEqualTo(1);
    }
    @Test
    public void testPart2() {
        // assertThat(day.part2(extension.getSampleLines())).isEqualTo(1);
        // assertThat(day.part2(extension.getLines())).isEqualTo(1);
    }
}